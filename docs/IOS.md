# iOS native (Tauri 2)

Worship Songbook iPhone shell uses the **same** React/Vite frontend as web / macOS / Windows.

Identifier: `studio.lyruma.worshipsongbook`  
Product name: Worship Songbook  
Author: Eduard Wiebe  
Copyright: Copyright 2026 Eduard Wiebe  
Version: keep in sync with `app/package.json` / `tauri.conf.json` / `Cargo.toml` / `appMeta.js` (currently **0.1.2**)

## Status

| Item | State |
|------|--------|
| Tauri iOS target | Initialized via `tauri ios init` (macOS / CI only — Linux CLI has no `ios` subcommand) |
| Xcode project | `app/src-tauri/gen/apple/` (generated; commit after first successful CI init) |
| Simulator CI | `.github/workflows/ios-native.yml` (workflow_dispatch) |
| Physical iPhone | **Not claimed** until a human device smoke-test passes |
| Signing / App Store | **Not enabled** — see Signing below |
| Git tag | No iOS release tag until device retest passes |

## Project layout (after init)

| Path | Role |
|------|------|
| `app/src-tauri/gen/apple/` | Xcode workspace / project generated by Tauri |
| `app/src-tauri/gen/apple/*.xcodeproj` | Open this in Xcode on your Mac |
| `app/src-tauri/Info.ios.plist` | Privacy usage strings (camera / photos) merged by Tauri |
| `app/src-tauri/capabilities/mobile.json` | iOS/Android capability set (HTTP + opener + secure storage) |

Concrete values from the successful simulator CI init/build (`33443656597`):

| Field | Value |
|-------|--------|
| Xcode project | `app/src-tauri/gen/apple/worship-songbook.xcodeproj` |
| Scheme | `worship-songbook_iOS` |
| Product name | Worship Songbook |
| Bundle ID | `studio.lyruma.worshipsongbook` |
| Deployment target | **iOS 14.0** |
| Version | 0.1.2 |
| Architecture (simulator CI) | **arm64** (`aarch64-sim`) |
| Architecture (device) | arm64 |
| Supported devices | iPhone (+ iPad orientations in Info.plist) |

Heavy rebuild outputs (`gen/apple/build`, `gen/apple/Externals`) are gitignored and regenerated by `tauri ios build`.

## Commands (macOS host required)

```bash
cd app
npm ci
# First time only:
npm run tauri:ios:init -- --ci
# Simulator debug build:
npm run tauri:ios:build:sim
# Or:
npx tauri ios build --debug --target aarch64-sim
```

API base for native builds: `VITE_API_BASE=https://songbook.lyruma.app` (default in `apiConfig.js` when native).

## Native Auth (iOS)

Same stack as desktop Tauri:

| Concern | Implementation |
|---------|----------------|
| Login / logout / refresh | `/api/auth/native/*` against `https://songbook.lyruma.app` |
| Access token | In-memory only |
| Refresh token | `secure_*` Tauri commands → **iOS Keychain** via `keyring` (`apple-native`) |
| Fallback | Process memory only if Keychain fails — **not** localStorage |
| Session restore | Refresh token from Keychain on cold start |

**Device Keychain behaviour is not claimed validated** until a real iPhone smoke-test. Simulator CI proves the binary links; it does not replace Keychain UX testing on device.

## Theme / language

- Theme Auto uses `prefers-color-scheme` (iOS system appearance) — no custom sensor logic
- Light / Dark preferences stored in `localStorage` (`songbook-theme`) — UI preference only; tokens stay in Keychain
- DE / EN via existing i18n catalog

## Desktop menu / updates on iPhone

- macOS menu bar is **not** installed on iOS (`nativeDesktop.js` skips mobile)
- “Check for Updates” on iOS returns `storeManaged` — App Store / TestFlight later; no blind GitHub Releases install path

## Permissions (minimal)

Configured in `Info.ios.plist` only for features that already exist:

| Key | Why |
|-----|-----|
| `NSCameraUsageDescription` | Scan-to-PDF (`capture="environment"`) |
| `NSPhotoLibraryUsageDescription` | Profile photo / scan gallery import |
| `NSPhotoLibraryAddUsageDescription` | Optional export to Photos |

No push notification entitlement. No unused location/mic permissions.

PDF / file import continues to use the HTML file input → iOS Files / document picker in WKWebView when available. No extra Tauri file dialog unless device testing shows a gap.

## External links

`openExternal` → `@tauri-apps/plugin-opener` (Safari / system handler). Same allow-list as desktop capabilities.

## Safe areas

`viewport-fit=cover` + `env(safe-area-inset-*)` on mobile top bar, bottom nav, content padding, and modal sheets.

`app/src/mobile-layout.css` (imported in `main.jsx`) adds:

| Area | Fix |
|------|-----|
| Top bar / content | Safe-area padding at `max-width: 1024px` (covers iPhone + iPad portrait) |
| Bottom nav | Hidden when modal open (`body:has(.modal-backdrop)`) |
| Drawer | `min(300px, 100vw - 40px)`, compact typography |
| Modals / forms | Full-width sheet, `max-height: 90vh`, scrollable body |
| Inputs | `font-size: 16px` on form controls at `<1200px` — prevents iOS focus auto-zoom |

## Viewport / keyboard corruption after modals (device test 2026-09-01)

**Symptom:** After opening a modal (Set/Band/Team), focusing an input, and closing the keyboard + modal, the underlying page stayed zoomed, shifted, or cropped with wrong effective viewport width. State persisted until manual pinch-zoom.

**Root causes (code analysis; WKWebView-specific — Chromium Playwright does not reproduce):**

| Factor | Detail |
|--------|--------|
| **iOS input auto-zoom** | `.field` labels use `font-size: 13px`; inputs inherit → effective `<16px` triggers WKWebView zoom on focus. Partial prior fix used `max(16px,1em)` blanket rule — replaced with explicit 16px on form controls only. |
| **autoFocus on modals** | Team / Set / Band / Scan dialogs used `autoFocus` → keyboard opens immediately on mobile, amplifying zoom corruption. |
| **No central modal lifecycle** | Inconsistent scroll lock (`body.style.overflow` in menu only); no blur or viewport restore after dismiss. |
| **Modal `dvh` units** | `max-height: 100dvh` could desync when keyboard opens/closes — changed to `vh` on mobile sheets. |

**Not involved:** No `body { position: fixed; top: -scrollY }` pattern found. No dynamic viewport meta or CSS/JS scale/zoom hacks.

**Fix:**

| File | Change |
|------|--------|
| `app/src/modalLock.js` | Central lock (overflow-only, no `position:fixed`), `dismissModal()`, `scheduleViewportRestore()` at 0/100/300/600ms, optional debug logging |
| `app/src/ModalBackdrop.jsx` | Shared modal shell with lock on mount / unlock on unmount |
| `app/src/useMobileFormFocus.js` | Skip `autoFocus` on mobile native / `≤767px` |
| `app/src/mobile-layout.css` | 16px on all mobile form controls; modal `vh` sizing |
| `app/src/App.jsx`, `AboutDialogs.jsx` | Modals migrated to `ModalBackdrop` + `dismissModal`; band editor blur/restore on close |

**Debug on device:** In Web Inspector console:

```js
localStorage.setItem('songbook-viewport-debug', '1')
```

Reload, then open/close modals. Console logs `[viewport:…]` with `innerWidth`, `clientWidth`, `scrollWidth`, `visualViewport.scale`, `offsetLeft`, etc. at open, focus, blur, dismiss, and +100/+300/+600ms.

**Device retest:** For each modal (Set anlegen, Band anlegen, Teammitglied hinzufügen): open → focus input → type → dismiss keyboard → close modal → verify `visualViewport.scale ≈ 1`, `offsetLeft ≈ 0`, `scrollWidth <= clientWidth`. Repeat open/close twice; no pinch-zoom needed.

Status marker when build + static tests green: `WORSHIP_SONGBOOK_IOS_VIEWPORT_KEYBOARD_MODAL_STATE_FIXED_READY_FOR_DEVICE_RETEST`

## Responsive overflow (device test 2026-09-01)

**Symptom:** Pages wider than iPhone viewport; user had to pinch-zoom out.

**Root causes (verified via Playwright overflow audit):**

| Element | Rule | Cause |
|---------|------|--------|
| `.song-tile-row` | `grid-auto-columns: minmax(420px, 1fr)` | Carousel grid forced 420px-wide columns; without `min-width: 0` on ancestors the grid expanded `document.scrollWidth` beyond the viewport |
| `.song-tile-row`, `.set-poster-row` | horizontal grid + `overflow-x: auto` | Missing `width/max-width: 100%; min-width: 0` — scroll container grew with content instead of clipping internally |
| `.app-shell`, `.content` | no `min-width: 0` | Flex/grid default `min-width: auto` propagated child minimum sizes upward |
| `.home-hero > .hero-background` | `width: calc(100% + 60px); transform: scale(1.08)` | Decorative scale extended past hero bounds (removed) |
| Prior fix | `overflow-x: clip` on `html/body` | Masked symptom only — removed |

**Fix:** `app/src/mobile-layout.css` — shell chain `min-width: 0`, carousel containment, breakpoint strategy (mobile `<768`, tablet `<1200`, desktop `≥1200`), modal viewport sizing. `extra.css` — 420px tile min removed, hero scale removed.

**Automated test:** `node scripts/test-overflow-playwright.mjs` (after `npm run build`, Playwright in `app/`).

**Device retest:** On each main page run `document.documentElement.scrollWidth <= document.documentElement.clientWidth`.

## Scan / OCR pipeline (iOS)

```
Camera / file → POST /api/scans → scan_to_pdf.py → song PDF
→ POST /api/songs/:id/analyze-chords → pdftotext and/or Tesseract OCR
```

| Step | iPhone-specific |
|------|-----------------|
| Capture | `prepareScanPages()` upscales JPEG to ≥2000px before upload |
| PDF build | `scan_to_pdf.py` EXIF transpose, autocontrast, min 2400px, sharpen |
| Analysis | Multi-PSM Tesseract @ 400 DPI; best candidate via `leadsheetAnalysis.mjs` |
| Quality | `needsReview: true` when score low / no chords detected — editor shows warning |

Desktop PDF import with text layer skips OCR; camera scans always OCR.

## Original PDF on iOS

**Symptom:** “Original-PDF” tab opened but stayed white after scan.

**Root cause:** WKWebView does not reliably render PDFs in `<iframe src="blob:…">`; blob MIME was often `application/octet-stream`.

**Fix:** `authorizedObjectUrl(..., { mimeHint: 'application/pdf' })` + `<embed type="application/pdf">` on iOS native (`AuthorizedMedia.jsx`).

## Device retest checklist (Eduard)

1. Fresh install development build on iPhone 17 Pro Max.
2. Login → Home: no horizontal overflow, no pinch-zoom needed.
3. Hamburger menu: fully visible, compact, close button reachable.
4. Team → Mitglied hinzufügen, Bands → Band anlegen, Sets → Set anlegen: modals fit viewport; keyboard does not hide primary actions.
5. Scan book page → verify Original-PDF tab shows scanned page.
6. Edit key tab: lyrics readable; if quality warning shown, manual correction expected.
7. Transpose/save in another key.
8. Repeat spot-check on iPad portrait + landscape.

Status marker when CI + local build green: `WORSHIP_SONGBOOK_IOS_RESPONSIVE_SCAN_EDITOR_STABILIZATION_READY_FOR_DEVICE_RETEST`


| | Simulator | Physical iPhone |
|--|-----------|-----------------|
| Signing | Not required | Apple ID + signing (see below) |
| CI | `ios-native.yml` | Manual Xcode on your Mac |
| Claim | “Simulator artifact built” only | Only after human smoke-test |

## Signing (do not enable without approval)

For a **physical device** install you typically need:

1. Apple ID signed into Xcode
2. Signing certificate (automatic managed by Xcode for development)
3. Provisioning profile (automatic for personal team / paid team)
4. Team ID in Xcode (and optionally `bundle.iOS.developmentTeam` in `tauri.conf.json` — **do not commit secrets**)
5. Device registered (free Apple ID: limited devices / 7-day cert expiry)

### Free Apple ID (local development)

- Possible for short-lived installs onto **your** iPhone via Xcode
- Apps expire (~7 days); limited devices
- Not for TestFlight / App Store / church-wide distribution

### Paid Apple Developer Program

- Required for TestFlight, App Store, longer-lived distribution, and many CI signing setups
- **Do not purchase / enroll without explicit approval**

Never commit `.p12`, provisioning profiles, or API keys.

## What you do on your Mac (next steps after Simulator CI is green)

1. Pull `main` (includes `gen/apple` once committed).
2. Open the Xcode project under `app/src-tauri/gen/apple/*.xcodeproj` (or the `.xcworkspace` if present).
3. Select scheme for Worship Songbook.
4. Choose your **physical iPhone** as run destination (not only a simulator).
5. Signing & Capabilities → Team = your Apple ID / Developer team; Bundler ID `studio.lyruma.worshipsongbook`.
6. Trust the developer certificate on the iPhone (Settings → General → VPN & Device Management) if prompted.
7. Run from Xcode (or `npx tauri ios dev` with the device selected).
8. Smoke-test: Login, session restore, theme, language, Bands, PDF import, profile photo, logout.

## Update strategy (iOS)

| Channel | Mechanism |
|---------|-----------|
| Public | App Store |
| Beta | TestFlight |
| Desktop | GitHub Releases metadata check (unchanged) |

Do not wire the desktop “Check for Updates” button to sideload IPAs on iOS.

## Explicit non-claims

- Not “iOS validated” until device smoke-test
- Not App Store ready
- Not universal claim that every PDF picker edge case works until tested on a real iPhone
- Android is out of scope for this mission
